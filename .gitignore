# Python & Bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
env.bak/
venv.bak/

# Django Specific
*.log
*.sqlite3
*.sqlite3-*
media/
staticfiles/
*.pot

# Environment variables
.env
.env.local
.env.*.local

# IDEs & OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Celery & Redis runtime state
celerybeat-schedule*
celerybeat.pid
dump.rdb
*.rdb

# Node (Tailwind)
node_modules/
npm-debug.log*

# Coverage
.coverage
htmlcov/

# Migrations are tracked deliberately: a fresh clone has to be able to build
# the exact schema the models expect. Ignoring them left every checkout with an
# empty database, and regenerating them per clone produced migrations that
# diverged from everyone else's.

requirements.txt

# Temporary secret-redaction files
.secret-replacements.txt

data/
.freebuff/
